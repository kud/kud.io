// Twitter reuses the per-project OpenGraph card verbatim (summary_large_image is
// set in the root metadata).
export {
  default,
  alt,
  size,
  contentType,
  generateStaticParams,
} from "./opengraph-image"
